'use client';
import { useState } from 'react';

export default function page() {
  const [checklistCompletion, setChecklistCompletion] = useState({});
  return (
    <main>
      <div>
        {/* checklist component */}
        {/* cant be bothered to make this save to db, will just save locally? */}
      </div>
    </main>
  );
}
